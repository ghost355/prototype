// Handlers/ControlHandler.swift

enum ControlHandler {
    static func execute(
        state: GameState,
        action: GameAction.Control,
        drawing: ActionCardDrawing
    ) -> GameState {

        // логика switch case

        return state
    }
}
