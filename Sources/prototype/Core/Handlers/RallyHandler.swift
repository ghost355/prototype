// Handlers/RallyHandler.swift

enum RallyHandler {
    static func execute(
        state: GameState,
        action: GameAction.Rally,
        drawing: ActionCardDrawing
    ) -> GameState {

        // логика switch case

        return state
    }
}
