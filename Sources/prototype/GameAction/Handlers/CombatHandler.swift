// CombatHandler.swift

enum CombatHandler {
    static func execute(
        state: GameState,
        action: GameAction.Combat,
        drawing: ActionCardDrawing
    ) -> GameState {

        // логика switch case

        return state
    }
}
