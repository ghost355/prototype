// EnemyHandler.swift

enum EnemyHandler {
    static func execute(
        state: GameState,
        action: GameAction.Enemy,
        drawing: ActionCardDrawing
    ) -> GameState {

        // логика switch case

        return state
    }
}
